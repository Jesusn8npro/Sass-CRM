/**
 * Constantes compartidas por varias páginas/componentes.
 * Lo que vive acá tiene que ser realmente reutilizado en >= 2 lugares.
 */

export const MONEDAS = ["COP", "USD", "ARS", "MXN", "EUR", "PEN", "CLP"] as const;
